import Section from '@/components/widgets/Section'

const OnBoarding = () => {
  return (
    <Section
    className={`pt-[12rem] -mt-[5.25rem] flex items-center justify-center w-full h-screen overflow-hidden`}
    crossesOffset={`lg:translate-y-[5.25rem]`}
    customPaddings
    id="onboarding">
      Onboarding
    </Section>
  )
}

export default OnBoarding
