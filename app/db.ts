// import mongoose from "mongoose";

// const key: string | undefined = process.env.NEXT_PUBLIC_MONGO_KEY;

// export default async function connect_db() {
//   try {
//     await mongoose.connect(key);
//     console.log("Connection with the database for onboarding is established");
//   } catch (error) {
//     console.log("Error ocured at db.ts (./app): ", error);
//   }
// }
